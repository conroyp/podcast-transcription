<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Search Tips Configuration
    |--------------------------------------------------------------------------
    |
    | Controls the content shown in the search tips / popular searches panel.
    | Theme-specific overrides can be defined in config/{theme}/search_tips.php
    | and will be deep merged with these base defaults.
    |
    */

    /*
    |--------------------------------------------------------------------------
    | Popular Search Examples
    |--------------------------------------------------------------------------
    |
    | Displayed as clickable pill links on the search page.
    | Each entry is a plain string; the view URL-encodes it when building links.
    |
    | Example:
    |   'They\'re just normal cheeses',
    |   'Helen copter',
    |
    */

    'popular_searches' => [
        // Add popular search terms here, or override in a theme-specific config file.
    ],
];
